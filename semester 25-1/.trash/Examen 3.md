Fase de MapReduce donde se lee un conjunto de datos y se genera una salida consistente en pares clave-valor (key-value):

Seleccione una:

a. Split

b. Map

c. Shuffling

d. Reduce

A la conexión de una red on-premise a una red virtual se le conoce como:

Seleccione una:

a. VPN Site-to-Site

b. VPN VNet-to-VNet

c. Red extendida.

d. Red regional.

Conjunto de reglas entrada y reglas de salida para los puertos, las cuales se definen para una subred o para una interfaz de red:

Seleccione una:

a. CIDR

b. NAT

c. NIC

d. NSG


Este tipo de balanceador de carga se utiliza para equilibrar la carga de las máquinas virtuales que ejecutan procesos de negocio que no son visibles al usuario externo:

Seleccione una:

a. Balanceador de carga interno

b. Balanceador de carga virtual

c. Balanceador de carga público

d. Balanceador de carga remoto


En un intervalo de direcciones IP CIDR:  

Seleccione una:

a. La dirección IP de la subred y la dirección IP de broadcast son direcciones IP no utilizables.

b. La dirección IP de la subred es la  dirección IP no utilizable.

c. La dirección IP de broadcast es la  dirección IP no utilizable.


¿Dónde se almacenan los blobs en Azure Blob Storage?

Seleccione una:

a. En recursos compartidos de archivos.

b. En un DBMS.

c. En discos de datos.

d. En contenedores.


¿A qué tarea de un job MapReduce corresponde cada descripción?

|                                                                                                                                                                               |                                                                                                |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| El HDFS distribuye los datos de entrada en los nodos slave que forman un cluster.                                                                                             | Respuesta 1Elegir...Split (dividir)Shuffling (barajar)Reduce (reducir)Merge (unir)Map (mapear) |
| Cada nodo slave ejecuta el procedo Reduce, el resultado se escribe en el HDFS.                                                                                                | Respuesta 2Elegir...Split (dividir)Shuffling (barajar)Reduce (reducir)Merge (unir)Map (mapear) |
| Los resultados intermedios son enviados a diferentes nodos slave, de acuerdo a la clave, de manera que los valores asociados a una misma clave sean puestos en el mismo nodo. | Respuesta 3Elegir...Split (dividir)Shuffling (barajar)Reduce (reducir)Merge (unir)Map (mapear) |
| Los resultados intermedios son agrupados por la clave.                                                                                                                        | Respuesta 4Elegir...Split (dividir)Shuffling (barajar)Reduce (reducir)Merge (unir)Map (mapear) |
| Cada nodo slave ejecuta el proceso Map, el resultado intermedio consistente en parejas clave-valor.                                                                           |                                                                                                |

Considere una instalación de NFS no seguro ¿Qué configuración se debe agregar en el archivo /etc/exports para que el cliente 192.204.100.10 pueda escribir y leer en el directorio /servidor/nfs del servidor NFS?

Seleccione una:

a. /servidor/nfs 192.204.100.10(ro,sync,no_subtree_check)

b. /servidor/nfs 192.204.100.10(rw,sync,no_subtree_check)

c. /servidor/nfs 192.204.100.10(insecure,sync,no_subtree_check)

d. /servidor/nfs 192.204.100.10(insecure,ro,sync,no_subtree_check)

En un sistema totalmente replicado ¿Qué hace el administrador de tráfico?

Seleccione una:

a. El administrador de tráfico funciona como un proxy transparente, el cual recibe las conexiones y peticiones de los clientes y las reenvía al sistema principal con copia a la réplica.

b. El administrador de tráfico funciona como un proxy, el cual recibe las conexiones y peticiones del servidor y las reenvía al sistema principal con copia a la réplica.

c. El administrador de tráfico funciona como un proxy transparente, el cual recibe las conexiones y peticiones de los clientes y las reenvía al sistema principal.

d. El administrador de tráfico funciona como un proxy transparente, el cual recibe las conexiones y peticiones de los clientes y las envía a la réplica.

Permite conectar redes virtuales entre sí o bien, redes virtuales con redes on-premise:

Seleccione una:

a. Puerta de enlace de red virtual.

b. Interfaz de red.

c. Túnel SSH.

d. Grupo de seguridad de red.

Para acceder un blob en Azure Blob Storage se requiere crear:

Seleccione una:

a. Un token de acceso privado.

b. Un token de acceso compartido.

c. Un usuario adminstrador.

d. Una clave pública y una clave privada.


¿Qué son los IOPS en una instancia de Azure Database for MySQL?

Seleccione una:

a. La cantidad de operaciones de lectura y escritura que la instancia puede realizar en un segundo.

b. El tipo de redundancia de la copia de seguridad de la instancia.

c. El período de retención de la copia de seguridad para una instancia.

d. El tamaño de la máquina virtual donde ejecuta la instancia.


El tamaño máximo del espacio de direcciones de una red virtual es:

Seleccione una:

a. /16

b. /32

c. /4

d. /8


Asegura que los datos respaldados se replicarán en una región a cientos de kilómetros de la región actual:

Seleccione una:

a. Punto de restauración

b. Geo-Redundant storage

c. DailyPolicy

d. Local-Redundant storage


En Azure Blob Storage ¿Cómo se revoca un token de acceso compartido?

Seleccione una:

a. Se debe cambiar la clave asociada al token.

b. No es posible revocar un token de acceso compartido, por tanto, se debe eliminar la cuenta de almacenamiento para que se puedan crear nuevos tokens.

c. No es posible revocar un token de acceso compartido, se debe esperar a que caduque el token.

d. Se debe cambiar las dos claves de la cuenta de la cuenta de almacenamiento.



Modelo de programación utilizado para procesar grandes cantidades de datos en el área de Big Data:

Seleccione una:

a. Data Warehouse

b. Modelo estrella

c. MapReduce

d. OLAP


¿Cuál es el nivel de acceso predeterminado en Azure Blob Storage?

Seleccione una:

a. Esporádico.

b. Archivo.

c. Frecuente.

d. Inactivo.

Distribuye la carga entre un grupo de servidores o recursos en el back-end:

Seleccione una:

a. VPN

b. Administración de tráfico.

c. NSG

d. Balance de carga en la nube.

En Azure, un disco de datos puede ser adjuntado o desasociado cuando la máquina virtual está encendida.

Seleccione una:

a. Solo si el disco es SSD.

b. Verdadero.

c. Solo si el disco es HDD.

d. Falso.


Considere una instalación de NFS sobre túnel SSH ¿Qué configuración se debe agregar en el archivo /etc/exports para que el cliente 192.204.100.10 pueda escribir y leer en el directorio /servidor/nfs del servidor NFS?

Seleccione una:

a. /servidor/nfs 192.204.100.10(insecure,rw,sync,no_subtree_check)

b. /servidor/nfs localhost(insecure,rw,sync,no_subtree_check)

c. /servidor/nfs localhost(ro,sync,no_subtree_check)

d. /servidor/nfs 192.204.100.10(ro,sync,no_subtree_check)

----


# 2


Cuando se crea una máquina virtual en Azure se crea por default:

Seleccione una:

a. Red virtual, subred, interfaz de red, IP privada, grupo de seguridad de red, IP pública.

b. Red virtual, interfaz de red, IP privada, grupo de seguridad de red, IP pública y disco de sistema operativo.

c. Red virtual, subred, IP privada, grupo de seguridad de red, IP pública y disco de sistema operativo.

d. Red virtual, subred, interfaz de red, IP privada, grupo de seguridad de red, IP pública y disco de sistema operativo.

Servicio de respaldos en la nube, que no requiere que el usuario cuente con el personal para realizar los respaldos, ni la instalación de una infraestructura de almacenamiento:

Seleccione una:

a. Azure Files

b. Azure Disk Storage

c. Azure Backup

d. Azure Blob Storage

En Azure Blob Storage, nivel de acceso online optimizado para almacenar datos que son accedidos y modificados frecuentemente. Este nivel de acceso es el más rápido y el más costoso.

Seleccione una:

a. Esporádico.

b. Archivo.

c. Inactivo.

d. Frecuente.


¿Qué debe hacerse para instalar un disco de datos en una máquina virtual?

Seleccione una:

a. 1) Adjuntar el disco, 2) formatear el disco, 3) montar el disco.

b. 1) Formatear el disco, 2) montar el disco, 3) adjuntar el disco.

c. 1) Montar el disco, 2) formatear el disco, 3) adjuntar el disco.

d. 1) Adjuntar el disco, 2) montar el disco, 3) formatear el disco.

¿Dónde se almacenan los archivos en Azure Files?

Seleccione una:

a. En contenedores.

b. En un DBMS.

c. En discos de datos.

d. En recursos compartidos de archivos.


26. PEndiendte

Sistema de procesamiento analítico en línea sobre una base de datos relacional:

Seleccione una:

a. RDBMS

b. ROLAP

c. Apache Hadoop

d. MapReduce

En Azure Blob Storage, nivel de acceso offline optimizado para almacenar datos que casi nunca son accedidos y con la posibilidad de ser accedidos con altas latencias (del orden de horas). Los datos en este nivel de acceso deben almacenarse como mínimo 180 días.

Seleccione una:

a. Archivo.

b. Esporádico.

c. Inactivo.

d. Frecuente.

Funciona como un proxy transparente el cual recibe las conexiones y peticiones de los clientes y las reenvía al sistema principal con copia a la réplica:

Seleccione una:

a. Balanceador de carga

b. Firewal

c. Grupo de seguridad de red

d. Administrador de tráfico

¿Qué hace la opción "sync" en la configuración del archivo /etc/exports?

Seleccione una:

a. Hace que las modificaciones realizadas por el cliente sobre los archivos se realicen en memoria y posteriormente se escriban en el disco del servidor.

b. Deshabilita la verificación de que el archivo que accede el cliente esté en el árbol de directorios exportado.

c. Hace que las modificaciones realizadas por el cliente sobre los archivos se escriban en el disco del servidor inmediatamente.

d. Permite que NFS use un puerto "inseguro", es decir, un puerto mayor o igual a 1024.

En un grupo de seguridad de red ¿qué reglas se aplican primero?

Seleccione una:

a. Las reglas que tienen menor prioridad.

b. Las reglas que tienen mayor prioridad.

c. Las reglas de puerto de entrada.

d. Las reglas de puerto de salida.


¿Qué tipo de redundancia de la copia de seguridad puede tener una instancia de Azure Database for MySQL?

Seleccione una:

a. Local o geográfica

b. Geográfica

c. Local

¿Cuántas direcciones IP tiene el intervalo 10.2.0.0/32?

Seleccione una:

a. 1

b. 32

c. 256

d. 1024

En un sistema totalmente replicado, para garantizar la recuperación del sistema principal, la réplica deberá:

Seleccione una:

a. Ejecutar el administrador de tráfico.

b. Ejecutar en una locación geográfica diferente al sistema principal.

c. Realizar un proceso de respaldo diario.

d. Ejecutar en el mismo site que el sistema principal.

En Azure, para montar un recurso compartido de archivos mediante SMB:

Seleccione una:

a. La cuenta de almacenamiento debe ser estándar.

b. El sistema operativo en la computadora debe ser Windows.

c. El sistema operativo en la computadora debe ser Linux.

d. La cuenta de almacenamiento debe ser prémium.

Está compuesta por uno o más espacios de direcciones IPv4 que no se superponen:

Seleccione una:

a. Grupo de seguridad de red.

b. Grupo de recursos.

c. Red virtual.

d. Puerta de enlace de red virtual.

En un sistema totalmente replicado ¿Qué hace el sistema principal?

Seleccione una:

a. El sistema principal procesa las peticiones de la réplica, y envía las respuestas al administrador de tráfico, el cual las reenvía al sistema principal.

b. El sistema principal procesa las peticiones de la réplica, y envía las respuestas al administrador de tráfico, el cual las reenvía al cliente.

c. El sistema principal procesa las peticiones del cliente, y envía las respuestas a la réplica, la cual las reenvía al cliente.

d. El sistema principal procesa las peticiones del cliente, y envía las respuestas al administrador de tráfico, el cual las reenvía al cliente.

Suponga que se ha instalado NFS en el servidor 192.204.200.10 y "ubuntu" es un usuario que puede acceder al servidor.  
  
Se configuró el archivo /etc/exports en el servidor para utilizar NFS sobre un túnel SSH.  
  
Si el cliente monta el directorio /servidor/nfs utilizando el comando:  
  
sudo mount -t nfs localhost:/servidor/nfs /cliente/nfs  
  
¿Qué instrucción se debe ejecutar en el cliente para subir el túnel SSH?

Seleccione una:

a. ssh -fNv -L 3049:192.204.200.10:2049 ubuntu@192.204.200.10

b. ssh -fNv -L 2049:localhost:2049 ubuntu@192.204.200.10

c. ssh -fNv -L 2049:192.204.200.10:2049 ubuntu@192.204.200.10

d. ssh -fNv -L 3049:localhost:2049 ubuntu@192.204.200.10

Máquinas virtuales que comparten la misma refrigeración, fuente de alimentación y red física:

Seleccione una:

a. Dominio de actualización

b. Región

c. Dominio de error

d. Conjunto de disponibilidad

¿Cuántas direcciones IP tiene el intervalo 10.2.0.0/16?

Seleccione una:

a. 256

b. 16

c. 1024

d. 65536


# 3

Permite conectar una máquina virtual a una red virtual:

Seleccione una:

a. Interfaz de red.

b. IP pública.

c. Grupo de recursos.

d. Grupo de seguridad de red.

En el área de Inteligencia de Negocios, tecnología que permite realizar análisis multidimensional de grandes volúmenes de datos de manera rápida y eficiente.

Seleccione una:

a. Apache Hadoop

b. OLAP

c. MapReduce

d. Data warehouse



Está compuesta por uno o más datacenters equipados con su propia alimentación, refrigeración y red:

Seleccione una:

a. Región

b. Red virtual

c. Zona de disponibilidad

d. Subred



En Azure Backup, contenedor lógico que almacena los datos del recurso a proteger:

Seleccione una:

a. Almacén de recovery services

b. Política de respaldo

c. Punto de restauración

d. Backup job

45. Pendiente

Azure Files no soporta el encriptado sobre el protocolo NFS, por tanto, la seguridad en la transferencia se basa en:

Seleccione una:

a. La seguridad de la red virtual.

b. Encriptado mediante clave pública y clave privada.

c. Un túnel SSH.

d. Encriptado mediante clave simétrica.



Para realizar el respaldo de una máquina virtual mediante Azure Backup, es necesario detener la máquina virtual:

Seleccione una:

a. Falso

b. Depende de la configuración de la máquina virtual

c. Verdadero

Tráfico de red entrante a un recurso:

Seleccione una:

a. Recarga

b. Carga

c. Descarga

d. Sobrecarga

Fase de MapReduce donde se lee los pares de clave-valor creados en la fase Map, entonces se genera un conjunto más pequeño de pares clave-valor:

Seleccione una:

a. Merge

b. Split

c. Reduce

d. Shuffling



Supongamos que tenemos dos computadoras con las siguientes IP: 192.30.50.20/22 y 192.30.48.10/22 ¿Estas computadoras pertenecen a la misma subred?

Seleccione una:

a. Sí.

b. No.

c. No es posible saberlo


En Azure Blob Storage:

Seleccione una:

a. Una clave en la cuenta de almacenamiento solo puede tener un token de acceso compartido.

b. Solo existe una clave en la cuenta de almacenamiento.

c. Una clave en la cuenta de almacenamiento puede tener varios tokens de acceso compartido.

d. Una cuenta de almacenamiento puede tener dos o más claves.


Supongamos que el proceso Reduce de MapReduce recibe tres claves diferentes, y para cada clave, una colección de valores:  
  
"1,2,1" 200.0 50.0  
"2,3,1" 100.0 200.0  
"1,2,3" 100.0  
  
Si el proceso Reduce suma los valores para cada clave, entonces Reduce produce la siguiente salida:

Seleccione una:

a. "1,2,1" 200.0  
"1,2,1" 50.0  
"2,3,1" 100.0  
"2,3,1" 200.0  
"1,2,3" 100.0

b. "1,2,1" 250.0  
"2,3,1" 300.0  
"1,2,3" 100.0

c. "1,2,1" 200.0 50.0  
"2,3,1" 100.0 200.0  
"1,2,3" 100.0


Contiene diferentes recursos de Azure:

Seleccione una:

a. Máquina virtual.

b. Grupo de seguridad de red.

c. Balanceador de carga

d. Grupo de recursos.


¿Qué hace el programa AdministradorTraficoSSL.java?

Seleccione una:

a. Utiliza sockets inseguros para recibir peticiones del cliente, y se conecta al sistema principal y a la réplica utilizando sockets seguros.

b. Implemementa sockets seguros para recibir peticiones del cliente, y se conecta al sistema principal y a la réplica utilizando sockets seguros.

c. Implemementa sockets seguros para recibir peticiones del cliente, y se conecta al sistema principal y a la réplica utilizando sockets inseguros.

d. Utiliza sockets inseguros para recibir peticiones del cliente, y se conecta al sistema principal y a la réplica utilizando sockets inseguros.


En Azure Database for MySQL, las instancias de mayor tamaño tienen más IOPS.

Seleccione una:

a. Verdadero

b. Depende de la versión de MySQL

c. Falso

Define cada cuándo se ejecuta el proceso de respaldo (backup job) y por cuánto tiempo se almacenarán los puntos de restauración:

Seleccione una:

a. Vault

b. Punto de restauración

c. Almacen de recovery services

d. Política de respaldo

Utilizando una VPN Site-to-Site, es posible conectar servidores on-premise a un balanceador de carga interno:

Seleccione una:

a. Solo si dicho bskanceador de carga tiene una IP pública

b. Falso

c. Verdadero

En Azure, para montar un recurso compartido de archivos mediante NFS:

Seleccione una:

a. La cuenta de almacenamiento debe ser estándar.

b. La cuenta de almacenamiento puede ser estándar o prémium.

c. Se debe deshabilitar la transferencia segura.

d. El sistema operativo en la computadora debe ser Windows.


¿Qué comando se utiliza para crear una clave pública y una clave privada para SSH?

Seleccione una:

a. ssh-keygen -t rsa -b 2048

b. keytool -genkeypair -keyalg RSA

c. ssh-key -t rsa -b 2048

d. keygen -t rsa -b 2048


El tamaño máximo y el tamaño mínimo de una subred es, respectivamente:

Seleccione una:

a. /32 y /8

b. /8 y /29

c. /8 y /32

d. /29 y /8


# 4
Suponga que se montó el directorio /cliente/nfs mediante el siguiente comando:  
  
mount -t nfs 192.204.200.10:/servidor/nfs /cliente/nfs  
  
¿Qué comando se utiliza para desmontar?

Seleccione una:

a. sudo umount /cliente/nfs

b. umount /cliente/nfs

c. umount /servidor/nfs

d. sudo umount /servidor/nfs

¿Qué es el período de retención de la copia de seguridad para una instancia de Azure Database for MySQL?

Seleccione una:

a. El tiempo durante el cual se conservan las copias de seguridad de la instancia.

b. Si los respaldos tendrán redundancia local o geográfica.

c. Si la conexión a la instancia utilizará SSL o no.

d. Cada cuando se realizará un respaldo de instancia.

Plataforma en Azure que permite almacenar cantidades masivas de datos no estructurados, a un costo reducido:

Seleccione una:

a. Azure Database for MySQL

b. Azure Disk Storage

c. Azure Files

d. Azure Blob Storage

¿Qué se configura en el firewall de una instancia de Azure Database for MySQL?

Seleccione una:

a. El intervalo de direcciones IP de las computadoras que podrán conectarse a la instancia, y el permiso para que cualquier recurso dentro de Azure pueda conectarse a la instancia.

b. Si la conexión a la instancia utilizará SSL o no.

c. El intervalo de direcciones IP de las computadoras que podrán conectarse a la instancia.

d. El permiso para que cualquier recurso dentro de Azure pueda conectarse a la instancia

Conjunto de datacenters interconectados mediante una red de baja latencia:

Seleccione una:

a. Zona de disponibilidad

b. Conjunto de disponibilidad

c. Región

d. Red virtual

En Azure Files:

Seleccione una:

a. Se utiliza la comunicación encriptada que ofrece NFS versión 4.

b. SMB permite montar un recurso compartido de archivos fuera de la red virtual.

c. SMB solo puede montar un recurso compartido de archivos dentro de la red virtual.

d. NFS permite montar un recurso compartido de archivos fuera de la red virtual.

En Azure Blob Storage, nivel de acceso online optimizado para almacenar datos que casi nunca son accedidos o modificados. Los datos en este nivel de acceso deben almacenarse como mínimo 90 días.

Seleccione una:

a. Archivo.

b. Inactivo.

c. Esporádico.

d. Frecuente.

Máquinas virtuales que se reiniciarán simultáneamente durante un mantenimiento planeado:

Seleccione una:

a. Dominio de actualización

b. Región

c. Dominio de error

d. Conjunto de disponibilidad

Servicio administrado de archivos compartidos en la nube de Azure:

Seleccione una:

a. Azure Disk Storage

b. Azure Files

c. Azure Blob Storage

d. Azure Database for MySQL


Subred que contiene las direcciones IP, que utilizan las máquinas virtuales que implementan una puerta de enlace de red virtual:

Seleccione una:

a. GatewaySubnet

b. Subred principal

c. SubnetGateway

d. Subred default


¿Qué comando se utiliza para montar el directorio remoto /servidor/nfs del servidor 192.204.200.10, en el directorio /cliente/nfs de la computadora local?

Seleccione una:

a. sudo mount -v -t nfs 192.204.200.10:/cliente/nfs /servidor/nfs

b. sudo mount -v -t nfs 192.204.200.10 /servidor/nfs /cliente/nfs

c. sudo mount -t nfs 192.204.200.10:/servidor/nfs /cliente/nfs

d. sudo mount -v 192.204.200.10:/servidor/nfs /cliente/nfs


Un túnel SSH es una canal encriptado que conecta:

Seleccione una:

a. Una máquina virtual con una red virtual

b. Una máquina virtual con otra máquina virtual

c. Un cliente con un servidor

d. Una red virtual con otra red virtual



Intervalo de direcciones IP definido en notación CIDR:

Seleccione una:

a. Espacio de direcciones IP.

b. Grupo de seguridad de red.

c. Intervalo de direcciones NAT.

d. Grupo de recursos.



En Azure Blob Storage, nivel de acceso que solo se puede establecer para el blob, no para la cuenta de almacenamiento:

Seleccione una:

a. Esporádico.

b. Frecuente.

c. Inactivo.

d. Archivo.


En Azure Blob Storage, nivel de acceso online optimizado para almacenar datos que son accedidos y modificados con poca frecuencia. Los datos en este nivel de acceso deben almacenarse como mínimo 30 días.

Seleccione una:

a. Frecuente.

b. Archivo.

c. Esporádico.

d. Inactivo.


Una configuración IP:

Seleccione una:

a. A veces contiene una IP privada.

b. Siempre contiene una IP privada.

c. Siempre contiene una IP privada y una IP pública.

d. siempre contiene una IP pública.


En Azure Blob Storage, si se tiene una URL de contenedor ¿Cómo se puede acceder a un blob en el contendor utilizando la URL de contenedor?

Seleccione una:

a. No es posible acceder un blob individual.

b. Solo es necesario quitar el nombre del contenedor a la URL de contenedor.

c. Solo es necesario agregar el nombre del blob a la URL de contenedor.

d. No es posible utilizar una URL de contenedor para acceder un blob, es necesario crear una URL para el blob.


Cuando un respaldo es borrado, Azure Backup retiene los datos del vault por un periodo de:

Seleccione una:

a. Un año

b. Un mes

c. Dos semanas

d. Una semana


¿Qué tipos de discos ofrece Azure?

Seleccione una:

a. HDD Standard, HDD Premium, SSD Standard, SSD Premium.

b. HDD Standard, HDD Premium, SSD Standard, SSD Premium y Ultra.

c. HDD Standard, SSD Standard y Ultra.

d. HDD Standard, SSD Standard, SSD Premium y Ultra


Cuando se crea una red virtual, se crea por default una subred llamada:

Seleccione una:

a. GatewaySubnet

b. default

c. VNet

d. subnet

